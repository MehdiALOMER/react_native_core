import styled from 'styled-components/native';
import { GenericViewPropTypes } from './genericViewPropTypes';

export const GenericView = styled.View<GenericViewPropTypes>`
  margin: 10px;
  padding:10px;
`;